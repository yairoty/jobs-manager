
// (paused, pending, running, finidshed/failed => date ?)

const JOB_STATUS = {
    PENDING: 'pending',
    RUNNING: 'running',
    FAILED: 'failed',    
    PAUSED: 'paused',
    FINISHED: 'finidshed'
}

module.exports = {
    JOB_STATUS
}